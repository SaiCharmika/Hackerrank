import numpy as np

# Set print options for correct alignment
np.set_printoptions(legacy='1.13')

# Read input values
n, m = map(int, input().split())

# Print the identity-like matrix
print(np.eye(n, m))
